# Test-only dependencies. Kept out of requirements.txt so they never reach the
# packaged .app, but declared here so a fresh clone (and CI) can actually run the
# suite — until now pytest existed only in whoever's venv had it installed by
# hand, which is why the first CI run failed with "No module named pytest".
-r requirements.txt

pytest>=9.1.1
# anyio is imported directly by tests that drive async MCP dispatch. It arrives
# transitively via starlette/httpx too, but a test dependency should be declared
# where it is used rather than relied on as somebody else's transitive.
anyio>=4.0
